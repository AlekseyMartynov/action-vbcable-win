name: Test

on:
  workflow_dispatch:
  push:

jobs:
  test:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    - run: |
        Expand-Archive .\VBCABLE_Driver_Pack43.zip
        .\VBCABLE_Driver_Pack43\VBCABLE_Setup_x64 -i -h

    - run: choco install fmedia

    - run: fmedia --list-dev
