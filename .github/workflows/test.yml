name: Test

on:
  workflow_dispatch:
  push:

jobs:
  test:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    - run: choco install vb-cable --version 43.0

    - run: choco install fmedia

    - run: fmedia --list-dev
